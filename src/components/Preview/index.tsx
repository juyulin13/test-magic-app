import React from 'react';
import './index.scss';
import { createWeiget, App } from '../../core/createWeiget'

export const Preview = (data: any) => {
  return (
    <div className='preview-container'>
      <App />
    </div>
  )
}